--- a/src/sapi/fpm/config.m4
+++ b/src/sapi/fpm/config.m4
@@ -147,6 +147,11 @@
         pid_t child;
         int status;
 
+        /* broken ptrace on Hurd, avoid hanging */
+        #ifdef __GNU__
+        return 10;
+        #endif
+
         if ( (child = fork()) ) { /* parent */
           int ret = 0;
 
